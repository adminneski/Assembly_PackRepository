 -- Cria o SEQUENCE para o campo [NFE_ID] da table [NFE_00]
 IF NOT EXISTS( SELECT * FROM SYS.OBJECTS WHERE TYPE = 'SO'AND OBJECT_ID = OBJECT_ID(N'SEQ_NFE_ID'))
  CREATE SEQUENCE dbo.SEQ_NFE_ID
  AS int 
  START WITH 1
  NO MINVALUE 
  NO MAXVALUE 
  NO CYCLE 
  CACHE 
  GO 
  
 -- "1058" Criando campo [NFE_ID_OLD] pra receber IDs do campo [NFE_ID] PARA GUARDAR OS IDs ANTIGOS 
 IF NOT EXISTS (SELECT 1 FROM INFORMATION_SCHEMA.COLUMNS 
 WHERE COLUMNS.TABLE_NAME = 'NFE_00'
 AND COLUMNS.COLUMN_NAME = 'NFE_ID_OLD')
 BEGIN 
 ALTER TABLE dbo.NFE_00
 ADD NFE_ID_OLD int NULL 
 END; 
 GO 
  
 -- "INTEGRACÃO" Criando campo [NFE_INTEGRACAO_OLD] pra receber BKP DAS INTEGRAÇÕES do campo [NFE_INTEGRACAO] 
 IF EXISTS ( SELECT 1 FROM INFORMATION_SCHEMA.COLUMNS 
             WHERE COLUMNS.TABLE_NAME  = 'NFE_00'
             AND   COLUMNS.COLUMN_NAME = 'NFE_INTEGRACAO')
 BEGIN 
    IF NOT EXISTS ( SELECT 1 FROM INFORMATION_SCHEMA.COLUMNS 
                    WHERE COLUMNS.TABLE_NAME = 'NFE_00'
                    AND COLUMNS.COLUMN_NAME  = 'NFE_INTEGRACAO_OLD')
    BEGIN 
       ALTER TABLE dbo.NFE_00 ADD NFE_INTEGRACAO_OLD VARCHAR(50) NULL 
    END; 
 END; 
 GO 
  
 -- "INTEGRACÃO" Guarda [NFE_INTEGRACAO] de integrações no novo [NFE_INTEGRACAO_OLD] bkp 
 IF EXISTS ( SELECT 1 FROM INFORMATION_SCHEMA.COLUMNS 
             WHERE COLUMNS.TABLE_NAME  = 'NFE_00'
             AND   COLUMNS.COLUMN_NAME = 'NFE_INTEGRACAO')
 BEGIN 
    UPDATE NFE_00
    SET    [NFE_INTEGRACAO_OLD] = NFE_INTEGRACAO
    WHERE  [NFE_INTEGRACAO_OLD] IS NULL 
 END; 
 GO 
  
 -- "1267" Guarda [NFE_ID] de relação com o cabeçalho no novo [NFE_ID_OLD] bkp 
 UPDATE NFE_00
 SET    NFE_ID_OLD = NFE_ID
 WHERE  NFE_ID_OLD IS NULL 
 GO
  
 -- Executa o SEQ_NFE_ID pra gerar os novos IDs do campo [NFE_ID] 
 UPDATE NFE_00
 SET    NFE_ID = NEXT VALUE FOR SEQ_NFE_ID
 GO 
  
 -- Recria índice Primary Key da table  [NFE_ID] para o campo [NFE_ID] 
 IF EXISTS ( SELECT count(table_name) 
    FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE 
    WHERE table_name = 'NFE_00'
    HAVING COUNT(*) > 1 ) 
 BEGIN
 -- DESABILITA INDICE 
 ALTER INDEX ALL ON [NFE_00]
 DISABLE 
  
 -- VERIFICA SE EXISTE O INDICE E O APAGA 
 IF  EXISTS (SELECT * 
             FROM sys.indexes AS si 
             WHERE si.name = 'NFE_00_PK')
             ALTER TABLE dbo.NFE_00
             DROP CONSTRAINT NFE_00_pk 
  
 -- ALTERA A TABLE CRIANDO O INDICE 
 ALTER TABLE dbo.NFE_00
 ADD CONSTRAINT NFE_00_pk 
 PRIMARY KEY CLUSTERED (NFE_ID) 
 WITH ( 
   PAD_INDEX = OFF, 
   FILLFACTOR = 90, 
   IGNORE_DUP_KEY = OFF, 
   STATISTICS_NORECOMPUTE = OFF, 
   ALLOW_ROW_LOCKS = ON, 
   ALLOW_PAGE_LOCKS = ON) 
 ON [PRIMARY] 
  
 -- REABILITA INDICES 
 ALTER INDEX ALL ON [NFE_00] 
 REBUILD 
 END
 GO